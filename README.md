# BuildSubmarines — Training Programs Prototype

Static site, no build step required.

## Pages
- `index.html` — Login screen (password: Alabama)
- `training-programs.html` — Training programs prototype
- `educators.html` — Education / Resources for Educators redesign, Direction A; gated by a login lightbox (login: anything, password: $ubs); assets in `educators-assets/`
- `educators-b.html` — Direction B, "the Monday-ready shelf": the same resources re-sorted by what they cost a teacher, as a five-rung commitment ladder. Editorial layout, same masthead/footer, same gate and assets.

## Deploy
Connected to Vercel via GitHub integration. Every push to `main` triggers a production deploy automatically.
